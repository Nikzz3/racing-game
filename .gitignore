node_modules/
dist/
server/data/
*.log
*.tsbuildinfo
.DS_Store
playwright-report/
test-results/

# Blender's local save backups; keep the editable .blend source.
*.blend[0-9]*

# Python (rl/)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv*/

# electron-builder output (desktop/dist is already covered by dist/ above).
desktop/release/

# Local environment (see .env.example, which stays tracked).
.env
.env.*
!.env.example
